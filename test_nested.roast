# Test nested function calls with globals

counter: int = 4

def inner_func() -> list[int]:
    print(f"Inner: counter = {counter}")
    result: list[int] = []
    for i in range(1, counter):
        print(f"  i = {i}")
        result.append(i)
    print(f"Inner: returning list with {len(result)} items")
    return result

def outer_func() -> str:
    print("Outer: calling inner...")
    items = inner_func()
    print(f"Outer: got {len(items)} items")
    return "OK"

def main() -> int:
    print("Main: calling outer...")
    result = outer_func()
    print(f"Main: result = {result}")
    return 0

main()
