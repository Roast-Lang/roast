# Fibonacci test - larger number
def fib(n: int) -> int:
    if n <= 1:
        return n
    return fib(n - 1) + fib(n - 2)

def main() -> int:
    result: int = fib(40)
    print(result)
    return 0
