# Super simple class test

class Point:
    def __init__(self, x: int, y: int):
        self.x: int = x
        self.y: int = y

    def sum(self) -> int:
        return self.x + self.y

def main():
    p = Point(10, 20)
    print(p.x)
    print(p.y)
    print(p.sum())
    print("Done!")
